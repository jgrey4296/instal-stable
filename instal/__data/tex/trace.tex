%% --------------------------------------------------
%  Institution Trace Start
%% --------------------------------------------------
\begin{longtable}{@{}l@{}}
  \resizebox{\textwidth}{!}{
    \begin{tikzpicture}[
      start chain=trace going right,
      %% state chain args
${args}
      node distance=1cm and 5.2cm
      ]
      { [continue chain=trace]
          %% state chain nodes
${nodes}
          %% Events
${events}
      }
      %% state chain bodies
${bodies}
      %% final linking
${links}
    \end{tikzpicture}
  }
\end{longtable}
